<Window x:Class="FencesWPF.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Renombrar"
        Width="300" Height="130"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF1A1A2E"
        Foreground="White"
        Loaded="Window_Loaded">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nuevo nombre:"
                   Foreground="#FFB0B0C0" FontSize="11" Margin="0,0,0,6"/>

        <TextBox x:Name="TbName" Grid.Row="1"
                 Margin="0,0,0,10"
                 KeyDown="TbName_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="80" Margin="0,0,8,0"
                    IsDefault="True" Click="BtnOk_Click"/>
            <Button Content="Cancelar" Width="80"
                    IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
